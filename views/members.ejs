<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ration Members</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/members.css">
</head>

<body>


    <div class="container mt-4">
        <a href="/" class="btn btn-outline-light mb-3">
            ← Back to Users
        </a>
    </div>

    <div class="container">

        <div class="card p-4 mb-4 bg-light">
            <h4 class="text-center section-title mb-4">Head of Family</h4>

            <div class="row text-center fw-bold">
                <div class="col-md-3">Name</div>
                <div class="col-md-3">House Name</div>
                <div class="col-md-3">Ration No</div>
                <div class="col-md-3">Category</div>
            </div>

            <div class="row text-center mt-3">
                <div class="col-md-3">
                    <%= ration.name %>
                </div>
                <div class="col-md-3">
                    <%= ration.houseName %>
                </div>
                <div class="col-md-3">
                    <%= ration.rationNo %>
                </div>
                <div class="col-md-3 fw-bold <%= ration.category === 'BPL' ? 'bpl' : 'apl' %>">
                    <%= ration.category %>
                </div>
            </div>
        </div>

        <div class="card p-4 mb-4 bg-light">
            <h4 class="text-center text-success mb-3">Add Family Member</h4>

            <form action="/add-member/<%= ration.rationNo %>" method="post">
                <div class="row">
                    <div class="col-md-5 mb-3">
                        <label class="form-label">Member Name</label>
                        <input type="text" name="memberName" class="form-control" required>
                    </div>

                    <div class="col-md-5 mb-3">
                        <label class="form-label">Relation</label>
                        <input type="text" name="relation" class="form-control" required>
                    </div>

                    <div class="col-md-2 d-flex align-items-end mb-3">
                        <button class="btn btn-success w-100">Add</button>
                    </div>
                </div>
            </form>
        </div>

        <div class="card p-4 bg-light mb-5">
            <h4 class="text-center text-primary mb-4">Family Members</h4>

            <% if (ration.members.length===0) { %>
                <p class="text-center text-muted">No members added yet</p>
                <% } else { %>

                    <div class="table-responsive">
                        <table class="table table-bordered table-hover text-center align-middle">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Member Name</th>
                                    <th>Relation</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% ration.members.forEach((member, index)=> { %>
                                    <tr>
                                        <td>
                                            <%= index + 1 %>
                                        </td>
                                        <td>
                                            <%= member.name %>
                                        </td>
                                        <td>
                                            <%= member.relation %>
                                        </td>
                                    </tr>
                                    <% }) %>
                            </tbody>
                        </table>
                    </div>

                    <% } %>
        </div>

    </div>


</body>

</html>